# 此 workflow 已停用
# 部署改由 Vercel 接管，不再使用 GitHub Pages
# 若需要重新啟用，請修改 on: 觸發條件

name: Deploy to GitHub Pages (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: '輸入 yes 確認手動部署'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "部署已改由 Vercel 接管，此 workflow 已停用"
